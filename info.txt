https://jekyllrb.com/docs/

gem install jekyll bundler

## Install the required bundles
bundle install 
ridk install


## Open the jekyll server with the current items
bundle exec jekyll serve

# Open the jekyll server with the future items
bundle exec jekyll serve --future

#check to be able to run this on a MAC